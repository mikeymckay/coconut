// Generated by CoffeeScript 1.6.2
(function(doc) {
  var dateData, day, month, visitDate, year;

  if (doc.IDLabel && doc.IDLabel !== "") {
    if (doc.VisitDate || doc.fDate) {
      if (doc.VisitDate) {
        visitDate = doc.VisitDate;
      }
      if (doc.fDate) {
        visitDate = doc.fDate;
      }
      dateData = visitDate.match(/(\d+)\/(\d+)\/(\d+)/);
      year = dateData[3];
      month = dateData[1];
      if (month.length === 1) {
        month = "0" + month;
      }
      day = dateData[2];
      if (day.length === 1) {
        day = "0" + day;
      }
      visitDate = "" + year + "-" + month + "-" + day;
      emit(visitDate, doc.IDLabel);
    }
  }
  if (doc.ClientID) {
    return emit(doc.createdAt, doc.ClientID);
  }
});

/*
//@ sourceMappingURL=map.map
*/
